package org.meetkt.catalogue.domain.model;

import org.meetkt.cart.domain.model.Item;

import java.util.Optional;

public class Catalogue {

    public Optional<Item> itemForm(String barcode) {
        return Optional.empty();
    }
}